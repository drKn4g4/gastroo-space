# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Legacy Hosting Preview (disabled)
on: pull_request
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_preview:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "Legacy Hosting PR preview disabled. Strategy B uses App Hosting as primary release channel."
